apiVersion: v1
kind: Service
metadata:
  name: client-cluster-ip-service
spec:
  selector:
    app: client-deployment-label
  type: ClusterIP
  ports:
    - name: client-cluster-ip-service-port-name
      protocol: TCP
      port: 3000
      targetPort: 3000 